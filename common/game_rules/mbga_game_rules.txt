# Which provinces may be demanded, how the transfer tool is reached, and how much work a
# state may cost when it opens.
#
# The first two are read only in common/scripted_guis: the mechanism itself knows nothing
# about what the game permits. The third is a machine setting rather than a rule of play and
# is read in common/script_values/mbga_values.txt, which is the only place it is named. It
# only ever applies on a map the mod has no lookup table for, since with one nothing is
# scanned at all.

mbga_province_access = {
	default = mbga_province_access_bordering

	mbga_province_access_bordering = {
	}

	mbga_province_access_anywhere = {
	}
}

mbga_province_tool = {
	default = mbga_province_tool_off

	mbga_province_tool_off = {
	}

	mbga_province_tool_diplomatic = {
	}

	mbga_province_tool_debug = {
	}
}

mbga_scan_budget = {
	default = mbga_scan_budget_standard

	mbga_scan_budget_low = {
	}

	mbga_scan_budget_standard = {
	}

	mbga_scan_budget_high = {
	}
}

mbga_ai_demands = {
	default = mbga_ai_demands_blocked

	mbga_ai_demands_blocked = {
	}

	mbga_ai_demands_allowed = {
	}
}
