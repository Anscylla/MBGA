# A country that exists for one purpose: to hold a single province for a fraction of a
# moment while the engine is asked whether we border it.
#
# It never appears on its own. It is created from one province, questioned, and the
# province is handed straight back, at which point it holds no land and ceases to exist.
# Having its own tag rather than borrowing somebody else's keeps every side effect and
# every log line attributable, and leaves real countries untouched.
MBG = {
	color = { 20 20 20 }

	country_type = unrecognized

	tier = city_state

	cultures = { north_german }
	capital = STATE_BRANDENBURG
}
