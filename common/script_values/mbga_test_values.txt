# Counts the provinces of a state the way the base game does it.
#
# common/script_values/occupation_values.txt is the only place in the base game that uses
# every_province, and it uses it exactly like this, from a state: "# root = state", beside
# state_population, testing has_label, which is a province trigger. So the engine does walk
# the provinces of a state - in a script value.
#
# This asks whether that walk really happens, by comparing the count with num_provinces,
# which the engine answers on its own.
#
# State scope. Test scaffolding: delete with the bench before release.

mbga_test_sv_provinces = {
	every_province = {
		add = 1
	}
}
