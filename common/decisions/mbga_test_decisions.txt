# Runs the iterator bench. A decision rather than a button because it needs no interface of
# its own to exist, works in any game, and leaves a mark in the decisions list that says
# plainly this is a test.
mbga_test_iterators_decision = {
	is_shown = {
		has_game_rule = mbga_province_tool_debug
	}

	possible = {
		exists = capital
	}

	when_taken = {
		mbga_test_run_iterators = yes
	}

	ai_chance = {
		base = 0
	}
}
