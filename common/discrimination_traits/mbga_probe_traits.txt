# The heritage and language named by mbga_probe_culture. Both are key references, not free
# identifiers: a culture naming ones that do not exist fails to load.

heritage_mbga_probe = {
	type = heritage
	trait_group = heritage_group_mbga_probe
}

language_mbga_probe = {
	type = language
	trait_group = language_group_mbga_probe
}
