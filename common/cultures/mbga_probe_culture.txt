# A culture for the probe countries, because a country definition needs one. Nothing else
# uses it and no pop ever has it. Its heritage and language are declared in
# common/discrimination_traits.

mbga_probe_culture = {
	color = { 20 20 20 }

	religion = atheist

	heritage = heritage_mbga_probe
	language = language_mbga_probe

	traditions = { }

	graphics = european
	ethnicities = {
		1 = neutral
	}

	# Prefixed, because a name is a localisation key of its own and a plain "Courier" would
	# be a global key this mod has no business claiming.
	male_common_first_names = {
		mbga_name_courier
	}
	female_common_first_names = {
		mbga_name_courier
	}
	common_last_names = {
		mbga_name_probe
	}
}
